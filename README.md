# EtherC-E- 
to code in EtherC includ  it in a  c file  like this 
#include "etherC.h"
this will  tell the file  to use  EtherC because EtherC has not  goten a conpiler 
it should  conpile like c so use gcc for now  
